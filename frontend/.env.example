# El frontend usa URLs relativas (/api/...) y el proxy de Vite o nginx
# maneja el ruteo al backend. No se necesita configurar nada aquí
# para desarrollo local ni para Docker.

# Solo cambiar si el backend corre en un host distinto al esperado:
# VITE_API_URL=http://otro-host:8000
